# DD_stock_bot — Unified Dependencies
# Install: pip install -r requirements.txt

# ── Stock Due Diligence Bot (YouTube pipeline) ───────────────
google-api-python-client>=2.0      # YouTube Data API v3
google-auth>=2.0                   # Google API auth
yt-dlp>=2024.0                     # Audio download from YouTube
openai-whisper>=20230314           # Local speech-to-text (Whisper)
requests>=2.28                     # HTTP requests (DeepSeek API)
reportlab>=4.0                     # PDF generation (pipeline analysis)

# ── Dexter Report Generator ─────────────────────────────────
openai>=1.0                        # OpenAI-compatible API client (DeepSeek)
fpdf2>=2.7                         # PDF report generation

# ── Shared / CLI ─────────────────────────────────────────────
tqdm>=4.60                         # Progress bars
python-dotenv>=1.0                 # Load .env file for API keys
